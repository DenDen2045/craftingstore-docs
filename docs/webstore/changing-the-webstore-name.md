#Changing the webstore name

You can modify the webstore name from the webstore settings.

1\. Go to "Settings" in the sidebar dashboard.  
2\. Click "Webstore"  
3\. Update the first option (store name)